# PHASE-BACKLOG — P6

Items **out of P6** unless user expands mid-run.

| Item | Notes |
|------|--------|
| Factory root `SKILL.md` / `github-publish/` | Markdown variant not updated in P6 |
| `scan-report.schema.json` | No schema change unless trim id needs new field |
| Re-init gate **C** alone (warn-only branch) | Handoff lists as UX option; slice 1 uses **A+B** only |
| `.mdc` conduct/safety dedup | Q6 replace/merge already; stretch only if cheap |
| Automated repo scanner | Still out of phase map |
| SDK / codebase reindex API | Unavailable — keep best-effort `read` once |
| Delete user ignore lines | Forbidden without confirmation |

## After P6 closeout

- Restore `PHASE-MAP.md` → `project_complete` if no P7
- Optional: link this run from `HANDOFF-idempotent-phase2-planner-seed.md` as resolved
