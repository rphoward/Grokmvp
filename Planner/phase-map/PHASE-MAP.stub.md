# PHASE-MAP (stub)

Rename or copy this file to `Planner/phase-map/PHASE-MAP.md` when you need a
**multi-phase** plan. For **single-phase** work, skip a disk phase map and embed
phase intent inline in the slice list per the unified contract.

## Replace with real sections

- `required_outcomes` — observable results for the current phase.
- `non_negotiable_intent` — scope the implementer must not dilute.
- `constraints` — hard rules and dependencies.
- `deferred_phases` — later phases with reasons and ordering (if any).

Validate the draft using `Planner/contracts/phase_map_review_prompt.md` before
authoring `SLICE-LIST.md`.
