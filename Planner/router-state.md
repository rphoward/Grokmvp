# Router state (Grokmvp)

Active-run pointer for Planner-routed work. Read this before any per-run `STATUS.md`.

```lisp
(router_state
  (active_run_dir   "Planner/slice-runs/factory")
  (active_status    "Planner/slice-runs/factory/STATUS.md")
  (active_phase     none)
  (updated          "2026-06-02")
  (notes            "Default factory run after clone. Grill Planner/intake/SPEC-SEED.stub.md then create_slice_list or single_phase_project. P1 proof archive: docs/references/grokmvp-phase1-proof/run/."))
```
