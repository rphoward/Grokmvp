# Router state (Grokmvp)

Active-run pointer for Planner-routed work. Read this before any per-run `STATUS.md`.

```lisp
(router_state
  (active_run_dir   "Planner/slice-runs/grokmvp-phase1")
  (active_status    "Planner/slice-runs/grokmvp-phase1/STATUS.md")
  (active_phase     "Phase 1")
  (updated          "2026-06-02")
  (notes            "grokmvp-phase1 P1 single_phase closeout complete (project_complete). Historical run pointer; start new slice-runs/<name>/ for P2+."))
```
